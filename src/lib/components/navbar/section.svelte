<ul>
    <slot/>
</ul>

<style>
    ul {
        margin-block: var(--br-navbar-section-margin-block);
        margin-inline: var(--br-navbar-section-margin-inline);
        padding-block: var(--br-navbar-section-padding-block);
        padding-inline: var(--br-navbar-section-padding-inline);
        gap: var(--br-navbar-section-gap);
        display: flex;
        list-style: none;
    }
</style>