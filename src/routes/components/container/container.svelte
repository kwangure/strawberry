<script>
	import { Docs } from '$internal';

	const description = [
		'The input Container adds layout and identity to input elements. It',
		'provides a canonical way to add labels, descriptions and validation',
		'to inputs.',
	].join('\n');
</script>

<Docs title='Input Container' {description}>
	<slot/>
</Docs>
